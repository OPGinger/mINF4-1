# Task 4 - Scenario 3: Two-Children Node Deletion

Run:

```powershell
& "../../../.venv/Scripts/python.exe" "szenario_zwei_kinder.py"
```

Outputs are generated in this folder only (`.gv`, `.pdf`, `antworten.txt`).
