# Task 4 - Scenario 1: Leaf Node Deletion

Run:

```powershell
& "../../../.venv/Scripts/python.exe" "szenario_blattknoten.py"
```

Outputs are generated in this folder only (`.gv`, `.pdf`, `antworten.txt`).
