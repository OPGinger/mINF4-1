# Task 4 - Scenario 2: One-Child Node Deletion

Run:

```powershell
& "../../../.venv/Scripts/python.exe" "szenario_ein_kind.py"
```

Outputs are generated in this folder only (`.gv`, `.pdf`, `antworten.txt`).
